@model FossTechCrm.Entities.Expanse

<h2>Delete Employee</h2>

@using (Html.BeginForm("delete", "Expanse", FormMethod.Post, new { id = "popupForm" }))

{
    @Html.HiddenFor(a => a.Id)
    <div class="form-group">
        <label>date</label>
        <p>@Model.DateOfSpend</p>
    </div>
    <div class="form-group">
        <label>Description</label>
        <p>@Model.Amount</p>
    </div>
    <div class="form-group">
        <label>Description</label>
        <p>@Model.Category</p>
    </div>
    <div class="form-group">
        <label>Description</label>
        <p>@Model.Currency</p>
    </div>
    <div class="form-group">
        <label>Description</label>
        <p>@Model.Marchant</p>
    </div>
    <div class="form-group">
        <label>Description</label>
        <p>@Model.Purpose</p>
    </div>
    <div class="form-group">
        <label>Description</label>
        <p>@Model.Reimbursable</p>
    </div>


    <input type="submit" value="Submit" />
}